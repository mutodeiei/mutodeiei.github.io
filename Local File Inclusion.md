
#  ___การจัดการช่องโหว่ Local File Inclusion (LFI)___ 
## เป้าหมาย: ไฟล์ rlfi.php ในระบบ bWAPP ระดับความยาก: ปานกลาง (Medium)
![This is an alt text.](/img/img2/63.png)

=== 

# 1. ฟังก์ชันการทำงานเดิม (Business Function)
### หน้านี้มีไว้ทำอะไร?

หน้านี้มีฟังก์ชันสำหรับ **เลือกภาษา (Language Selection)** เพื่อปรับเปลี่ยนการแสดงผลข้อความต้อนรับบนหน้าเว็บให้ตรงตามความต้องการของผู้ใช้งานแต่ละเชื้อชาติ

**กลไกการทำงาน:**
1.  **การรับค่า:** ผู้ใช้เลือกภาษาจากรายการที่มี (เช่น English, French)
2.  **การส่งข้อมูล:** ระบบจะส่งชื่อไฟล์ภาษาผ่านพารามิเตอร์ใน URL (เช่น `language=lang_en.php`) เพื่อบอกเซิร์ฟเวอร์ว่าต้องการแสดงผลภาษาอะไร
3.  **การประมวลผล:** เซิร์ฟเวอร์จะทำการโหลดไฟล์นั้นขึ้นมาแสดงผลทันที

**สิ่งที่ควรจะเป็น (Security Expectation):**
ระบบควรจำกัดให้เข้าถึงได้เฉพาะไฟล์ภาษาที่จัดเตรียมไว้เท่านั้น และต้องไม่อนุญาตให้ผู้ใช้ป้อนชื่อไฟล์อื่นๆ ในระบบได้เอง

---

# 2. ช่องโหว่และการทดสอบ (Vulnerability & Payload)
### ปัญหาอยู่ที่บรรทัดไหน?

จากการสแกนด้วยเครื่องมือ RIPS พบว่าโค้ดมีการเขียนที่ไม่ปลอดภัย โดย RIPS แจ้งเตือนดังนี้
![This is an alt text.](/img/img2/63.png)

#### ผลการสแกน (RIPS Output)
> Userinput reaches sensitive sink
>> + 94: include($language); (จุดระเบิด/Sink)
>> + 29: $language = $_GET["language"]; (จุดรับเชื้อ/Source)
>> requires
>>> + 80: if(isset($_GET['language']))
>>> + 91: if(in_array($language, $allowed_files))

### คำอธิบายปัญหา (Root Cause Analysis)

คำถามสำคัญคือช่องโหว่นี้เกิดขึ้นได้อย่างไร? คำตอบคือ **"ความไว้วางใจในการป้อนข้อมูลของผู้ใช้ (User Input Trust)"** มากเกินไป

1.  **จุดรับข้อมูล (Source) - บรรทัดที่ 29:**
    ตัวแปร `$language` รับค่ามาจาก URL (`$_GET`) โดยตรง ค่านี้แฮกเกอร์สามารถกำหนดเองได้ (เช่น เปลี่ยนจากภาษาเป็นที่อยู่ไฟล์สำคัญ)
2.  **จุดที่ข้อมูลถูกนำไปใช้ (Sink) - บรรทัดที่ 94:**
    นี่คือจุดที่อันตรายที่สุด! คำสั่ง `include($language);` จะนำค่าที่รับมาไป **"รัน"** หรือ **"เปิดอ่าน"** ทันที หากเราส่งที่อยู่ไฟล์ระบบเข้าไป เซิร์ฟเวอร์ก็จะไปเปิดไฟล์นั้นมาให้เราดู
3.  **การตรวจสอบที่ไม่เพียงพอ:**
    แม้จะมี `in_array` แต่หากตัวกรองนี้ไม่ครอบคลุมหรือมีช่องโหว่ในการเขียนโปรแกรม (เช่น ยอมให้มี `../`) แฮกเกอร์ก็จะสามารถใช้เทคนิค **Directory Traversal** หรือการ "เดินถอยหลัง" ออกจากโฟลเดอร์ปัจจุบันไปสู่ชั้นลึกสุดของระบบ (Root) ได้

---

# 3. การพิสูจน์ช่องโหว่ (Exploitation)
### ขั้นตอนการเจาะระบบ (Step-by-Step)

เพื่อให้เห็นภาพการทำงาน เรามาลองทำตามขั้นตอนของแฮกเกอร์กันครับ:

1.  **การวิเคราะห์ URL:** สังเกตว่าเว็บใช้พารามิเตอร์ `?language=lang_en.php`
2.  **การทดสอบไอเดีย:** แฮกเกอร์จะลองเปลี่ยนชื่อไฟล์เป็นไฟล์อื่นที่น่าจะมีอยู่ในระบบ เช่น `?language=/etc/passwd`
3.  **การใช้เทคนิค Directory Traversal:** เนื่องจากเราไม่รู้ว่าโค้ดตัวนี้เก็บอยู่ในโฟลเดอร์ลึกแค่ไหน เราจึงใช้สัญลักษณ์ `../` เพื่อ "ย้อนกลับ" ออกไปทีละชั้น
    *   **Payload ที่ใช้:** `../../../../etc/passwd`
4.  **จุดประสงค์:** ตัว `../` จะพาเราออกจากเว็บแอปพลิเคชัน ไปสู่ระดับระะบบปฏิบัติการ (OS) จนถึงไฟล์ `/etc/passwd` ซึ่งเก็บรายชื่อผู้ใช้ทั้งหมดของ Linux
5.  **ผลลัพธ์ที่ได้:** เมื่อกด Enter หน้าเว็บจะเปลี่ยนจากคำชมเชยภาษาต่างๆ กลายเป็นรายชื่อผู้ใช้งานระบบทันที ยืนยันว่าเราสามารถ "เจาะ" เข้าไปดูความลับของเซิร์ฟเวอร์ได้แล้ว

---

# 4.แนวทางการแก้ไข (Remediation)
### แก้ยังไงให้หายขาด? (Best Practice)

เราเปลี่ยนวิธีคิดจาก "การพยายามตรวจจับสิ่งที่ไม่ดี" เป็น "การอนุญาตเฉพาะสิ่งที่ดี" (Strict Whitelisting) โดยใช้คำสั่ง `switch...case` ซึ่งเป็นการล็อคชื่อไฟล์ที่อนุญาตไว้ในโค้ดโดยตรง (Hardcoded)

**ทำไมวิธีนี้ถึงปลอดภัย?**
1.  **ตัดการเชื่อมต่อกับ Input:** พารามิเตอร์ `language` จาก URL จะถูกนำมาเปรียบเทียบเท่านั้น ไม่ได้ถูกนำไปเปิดไฟล์โดยตรงอีกต่อไป
2.  **Whitelist เท่านั้น:** หากแฮกเกอร์ส่ง `../etc/passwd` เข้ามา มันจะไม่ตรงกับเคสใดๆ ที่เราเตรียมไว้ (เช่น `lang_en.php`)
3.  **Default Case:** เมื่อข้อมูลไม่ตรงกับที่กำหนด ระบบจะเข้าสู่โหมด `default` ทันที ซึ่งเราเขียนดักไว้ให้แสดง "Access Denied" แทนการเปิดไฟล์

```php
switch($_GET["language"]) {
    // ✅ อนุญาตเฉพาะไฟล์ที่เรารู้จักและเตรียมไว้ให้เท่านั้น
    case "lang_en.php":
        include("lang_en.php");
        break;
    case "lang_fr.php":
        include("lang_fr.php");
        break;
    case "lang_nl.php":
        include("lang_nl.php");
        break;
    
    // ❌ กรณีอื่นๆ ทั้งหมด (รวมถึง Payload ของแฮกเกอร์) จะถูกส่งมาที่นี่
    default:
        echo "<font color='red'>Access Denied</font>";
        break;
}
```
# โค้ดที่ยังไม่แก้ไข
![This is an alt text.](/img/img2/64.jpg)

# โค้ดที่แก้ไขแล้ว
![This is an alt text.](/img/img2/65.jpg)
---

# 5. การตรวจสอบหลังแก้ไข (Verification)
### มั่นใจได้อย่างไรว่าปลอดภัย?

หลังจากแก้ไขโค้ดแล้ว เราต้องทำการทดสอบ 3 ด้านเพื่อให้มั่นใจ 100% ว่าระบบทำงานถูกต้อง:

## 5.1 ด้านความปลอดภัย (Security Check)
*   **วิธีทดสอบ:** ลองป้อน Payload เดิม: `../../../../etc/passwd` เข้าไปที่พารามิเตอร์ URL
*   **สิ่งที่คาดหวัง:** ระบบต้องไม่แสดงข้อมูลในไฟล์ แต่ต้องขึ้นข้อความ **"Access Denied"** สีแดงแทน
![This is an alt text.](/img/img2/66.png)

## 5.2 ด้านคุณภาพโค้ด (Source Code Analysis)
*   **วิธีทดสอบ:** นำโค้ดที่แก้ไขแล้วไปสแกนด้วยเครื่องมือ RIPS อีกครั้ง
*   **สิ่งที่คาดหวัง:** RIPS ต้องขึ้นแถบสีเขียวพร้อมข้อความ **"No vulnerabilities found"** เพื่อยืนยันว่าไม่มีช่องทางไหลของข้อมูลที่อันตรายแล้ว
![This is an alt text.](/img/img2/68.png)

## 5.3 ด้านการใช้งานทั่วไป (Usability Check)
*   **วิธีทดสอบ:** ลองกดเลือกภาษา "English" หรือ "French" จากเมนูหน้าเว็บตามปกติ
*   **สิ่งที่คาดหวัง:** เว็บไซต์ต้องแสดงผลภาษาได้ถูกต้อง ไม่เกิด Error หรือปัญหาการใช้งานใดๆ
![This is an alt text.](/img/img2/67.png)

===

# สรุป :
การดำเนินการแก้ไขช่องโหว่ Local File Inclusion (LFI) บนระบบ bWAPP เสร็จสิ้นสมบูรณ์ การปรับปรุงโค้ดด้วยวิธี Strict Whitelisting สามารถขจัดความเสี่ยงจากการเข้าถึงไฟล์ระบบโดยไม่ได้รับอนุญาตได้ 100% โดยผ่านการตรวจพิสูจน์ทั้งในเชิงรุก (Exploitation) และเชิงรับ (Source Code Analysis) เรียบร้อยแล้ว ระบบมีความมั่นคงปลอดภัยพร้อมใช้งานตามปกติ